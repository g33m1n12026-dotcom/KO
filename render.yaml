services:
  - type: web
    name: kindle-ai-cloud
    runtime: node
    plan: free
    region: frankfurt
    buildCommand: npm run build
    startCommand: node dist/server.cjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: GEMINI_API_KEY
        sync: false
